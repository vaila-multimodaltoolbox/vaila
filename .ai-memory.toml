# ai-memory routing marker for this repository.
# Runtime server settings live in ai-memory's per-user data directory; this
# file only keeps every worktree and subdirectory in the same memory scope.
workspace = "default"
project = "vaila"
