# Copy to `.env` in the project root for local development only.
# `.env` is gitignored — never commit real secrets.
#
# Example (uncomment and set if your tooling reads these):
# OPENAI_API_KEY=
# Some tools use provider-specific env vars; see each tool's documentation.
